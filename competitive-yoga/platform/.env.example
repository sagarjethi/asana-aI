# Yoga Drishti platform — environment
DATABASE_URL=postgres://localhost:5432/yoga_drishti
JWT_SECRET=dev-only-change-me
# Set to "1" to run the scoring pipeline against bundled sample keypoints (no camera/DB needed)
NEXT_PUBLIC_DEMO_MODE=1
